\begin {align* }
\mathcal {J} &:= \text{Jobs (each job Sj$ is one aircraft maintenance
task)} \\
\mathcal{P} &:= \text{Positions (hangar stands), e.g., }\{1,2,3,4,5\} \\
\mathcal {R} &:= \text{Planes (aircraft identifiers)} \\
\mathcal {C} &:= \text{CIients} \\
\mathcal{S} &:= \text{Discrete time slots in days, } \
{0,\ldots,S_{\max}-l\} \\
\mathcal {I}A } &:= \text{Pairs of positions that cannot
overlap at entry }(p Nin \mathcal {P})
\mathcal {I}A } &:= \text {Pairs of positions that cannot
overlap at exit }(p \in \mathcal{P}).
\end{align*}